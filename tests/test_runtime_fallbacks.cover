    1: from evaluator.nodes.extractor import run_extractor
       
       
    1: def test_run_extractor_falls_back_to_heuristic_when_llm_fails(monkeypatch):
    2:     monkeypatch.setattr("evaluator.nodes.extractor.llm_extract", lambda state: (_ for _ in ()).throw(RuntimeError("boom")))
    2:     result = run_extractor(
    1:         {
    1:             "session_id": "S_fallback",
    1:             "conversation": "human: Eu sou Ana\nai: Olá, Ana! Posso te enviar o material.",
               }
           )
    1:     assert result["extracted_facts"].metadata.session_id == "S_fallback"
